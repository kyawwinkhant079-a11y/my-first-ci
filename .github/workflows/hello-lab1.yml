name: Lab 1 - My First Automation

on: [push]

jobs:
  my-jobs: 
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print My Goal
        run: echo "Lab 1 success"
    
      - name: Show Current Time
        run: date